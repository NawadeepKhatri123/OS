functions

void simpletron.loadfromuser();
void simpletron.loadfromfile();
void simpletron.executeprogram();
void Dumpcore();

int memory[10000]
int accumulator{0};
int instruction_counter{0}
int index register{0}
int instruction register{0}

int main(){
    print("welome to simpletron")
    Prompt the user for choice for whether to upload a file or enter manually
    if input is from the user then{
        simpletron.loadfromuser();
    }else{
        simpletron.loadfromfile();
    }
    simpletron.executeprogram();

}

void simpletron.loadfromuser(){
    - initialize a variable for counter to add on to the meomry locations
    - create a while loop for continuous data entry
    - prompt the user to input data
    - make sure the input is in the range of -999999 to 999999 and check if the input is anything else than instruction
    - if the input is "GO" then {
        return 0;
    }else{
        memory[variable++]
    }
}

void simpletron.executeprogram(){
    -initalize false boolean variable
    - while(halt){
        instruction register = memory[instruction counter++]

        Since the instructions are six digits and we only need the first and the last one we:

        first2 = instruction register /10000
        last2 = instruction register % 10000

        switch (first2)

            case Read;
                std::cout<<"Enter a number"
                std::cin>> memory[last2]
                break;

            case write;
                print "OUTPUT" memory[last2]
                break;

            case halt;
                print "program halted"
                dumpcore();
                halt = true;
                break;


            ....................
            ...................
            ............
            ...................
            ...............
            ...........
            .....
    }
}

void dumpcore(){

    FOR TOMORROW;
}
